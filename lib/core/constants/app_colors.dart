import 'package:flutter/material.dart';

class AppColors {
  // Primary: Sage Green (chữa lành, thư giãn, thiên nhiên)
  static const Color primary = Color(0xFF4E736E);
  static const Color primaryLight = Color(0xFF7E9F9B);
  static const Color primaryDark = Color(0xFF2C4A46);

  // Background & Surface: Warm Cream & Soft tones
  static const Color background = Color(0xFFF9F9F6);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF0F3F1);

  // Accent & Emotion indicators
  static const Color accentCoral = Color(0xFFE07A5F); // Cờ đỏ / cảnh báo căng thẳng
  static const Color accentAmber = Color(0xFFF4A261); // Mức độ vừa
  static const Color accentLavender = Color(0xFF818AA3); // Tâm trạng bình an / tĩnh
  static const Color accentTeal = Color(0xFF2A9D8F); // Tích cực / ổn định

  // Text colors
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);

  // Borders & Dividers
  static const Color border = Color(0xFFE2E8F0);

  // Dark Theme Palette (Obsidian & Serene Slate)
  static const Color darkBg = Color(0xFF0E1418);
  static const Color darkBgCenter = Color(0xFF162329);
  static const Color darkSurface = Color(0xFF19252B);
  static const Color darkCard = Color(0xFF1D2B32);
  static const Color darkBorder = Color(0x24FFFFFF);
  static const Color darkTextPrimary = Color(0xFFF1F5F9);
  static const Color darkTextSecondary = Color(0xFF94A3B8);
  static const Color darkTextMuted = Color(0xFF64748B);
}
